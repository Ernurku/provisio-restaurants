// Шаблон конфига Supabase. Скопируйте этот файл в js/supabase-config.js
// (он в .gitignore и никогда не попадёт в git) и подставьте свои значения
// из Supabase Dashboard → Project Settings → API.
//
// URL и anon key — публичные по своей природе (это не service_role и не
// пароль), но в этом репозитории они всё равно вынесены из кода, чтобы
// в git не попадали значения конкретного проекта.
var SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
var SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';
